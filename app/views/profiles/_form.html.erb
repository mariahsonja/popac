<%= form_with(model: profile, url: profile_path, local: true) do |form| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :profile_name %>
  </div>

  <div class="field">
    <%= form.label :bio %>
    <%= form.text_area :bio, id: :profile_bio %>
  </div>

  <div class="field">
    <%= form.label :country_id %>
    <%= form.collection_select :country_id, @countries, :id, :name %>
  </div>
  
  <div class="field">
    <%= form.label :country_id %>
    <%= form.collection_select :country_id, @countries, :id, :name %>
  </div>


  <div class="field">
    <%= form_for(@interests) do |f| %>
    <%= f.fields_for :interests do | pt | %> 
    <%= pt.label :interests, "Tags" %>
    <%= pt.collection_select(:interests_id, Interests.all, :id, :name, {include_hidden: false}, {multiple: true} ) %>
    
    <select multiple data-role="tagsinput">
      <option value="Child">Child</option>
      <option value="Refugees">Refugees</option>
      <option value="Homeless">Homeless</option>
      <option value="Education">Education</option>
      <option value="Art">Art</option>
    </select>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
